pipeline {
    agent any

    tools {
        nodejs "NodeJS 18"
    }

    stages {
        stage('Build') {
            steps {
                sh 'node -v'
                sh 'npm install'
                // perintah lainnya
            }
        }
    }
}
